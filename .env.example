

# KobeCrypto — Exemple d’environnement (aucun secret)
# Copiez ce fichier en `.env` pour un usage local si besoin.

# Mode d’exécution
KOBE_ENV=dev
PYTHONUNBUFFERED=1

# Dossiers locaux (restent non versionnés)
LOG_DIR=logs
DATA_DIR=data

# WebSocket public Binance (aucune clé requise pour v0)
BINANCE_WS_BASE=wss://stream.binance.com:9443

# Horloge / fuseau (affichage uniquement)
TZ=UTC